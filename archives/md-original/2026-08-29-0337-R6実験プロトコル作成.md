# R6実験プロトコル作成

- 作成日時 (UTC): 2026-08-29T00:17:17.0000000Z
- 更新日時 (UTC): 2026-08-29T02:37:56.0000000Z
- 注記: 本文は公開用に秘密形式・メールアドレス・電話番号のみ不可逆に伏せ字化。

## user / 2026-08-29T00:17:15.0000000Z

```
新規研究Track：

R6 — Multidimensional Query Intelligence

既存R0〜R5は変更禁止。
新規専用branch/worktreeを作成。

目的：

「全情報を毎回処理する」のではなく、
State / Goal / Relation / Context等から正しいQueryを生成して必要情報だけ取得することで、同等Task Successをより少ない情報アクセス量・演算量で達成できるか検証する。

まずExperiment ProtocolとSynthetic Relational Worldを作る。

まだ結果に合わせた最適化は禁止。

World

最低10,000 relations / world。

各relationに独立したdimensionを持たせる。

候補：

* entity
* relation type
* time
* owner
* location
* context
* provenance
* state
* goal relevance

TaskごとのGold answerは少数relationのcompositionから決定。

必要Gold relationsをgeneratorだけが保持。

SolverにはGold relation IDsを渡さない。

Dataset

最低：

* Training/Development用 1,000 tasks
* sealed Holdout 1,000 tasks

Holdoutは生成・SHA256 Freezeのみ。
評価禁止。

Comparison

A. FULL_SCAN
全relationsを処理。

B. SINGLE_DIM_QUERY
1 dimensionのみでfilter。

C. MULTI_DIM_QUERY
複数dimensionからqueryを構成。

Primary Metrics

* Task Success
* relations accessed / task
* bytes accessed / task
* candidate set size
* query generation operations
* total operations
* CPU time
* peak memory

Efficiency Metric

単なるAccuracyだけでなく、

Information Access Ratio = accessed relations / total relations

を保存。

Critical controls

MULTI_DIMだけGold情報を持つことは禁止。

全systemに同じTask input / State / Goalを与える。

Query keyにGold answer、Gold relation ID、difficulty labelを入れない。

Dimension Ablation

Developmentで：

* 1 dimension
* 2 dimensions
* 3
* 4
* 5+

を比較可能にする。

どのdimension combinationを使ったか保存。

Evidence

R0 Evidence Contract準拠。

まずProtocol + generator + leakage auditまで。

結果を見てProtocol変更禁止。

成果物：

R6_MULTIDIMENSIONAL_QUERY_PROTOCOL.md

最後に：

R6_PROTOCOL_READY = YES / NO

GPU禁止。
```

## assistant / 2026-08-29T00:17:31.0000000Z

```
R0〜R5とEvidence Contract・命名規約を確認後、専用branch/worktreeでProtocol・generator・leakage auditを実装します。Holdoutは生成・SHA256固定までで封印し、Developmentのみ評価可能にします。
```

## assistant / 2026-08-29T00:18:05.0000000Z

```
null
```

## assistant / 2026-08-29T00:18:06.0000000Z

```
null
```

## assistant / 2026-08-29T00:19:03.0000000Z

```
null
```

## assistant / 2026-08-29T00:19:13.0000000Z

```
null
```

## assistant / 2026-08-29T00:20:16.0000000Z

```
null
```

## assistant / 2026-08-29T00:20:41.0000000Z

```
DevSpaceは認証エラーのため、GitHub側で既存構造と契約を確認しました。R6はmain基点とし、生成物・manifest・raw counts・実コマンド・生ログを含む監査証跡まで揃えます。
```

## assistant / 2026-08-29T00:29:52.0000000Z

```
専用branch `rebuild/r6-multidimensional-query` を作成済みです。生成器ではTrain/DevとHoldoutを別World化し、Holdout seed・Gold mappingを隔離、9次元全511組合せを事前固定しました。
```

## assistant / 2026-08-29T00:32:50.0000000Z

```
null
```

## assistant / 2026-08-29T00:39:05.0000000Z

```
CPU限定の本番生成とauditは完了し、15/15 PASS、Holdout評価・Task Success計算は0件です。次はEvidence再検証とbyte-identical Freeze確認を進めます。
```

## assistant / 2026-08-29T00:42:25.0000000Z

````
一番大きい穴は、**ローカルでは生成・監査まで動いているが、まだGitHub上の再現可能なEvidenceとして固定できていないこと**です。

## できているもの

ローカル作業領域には、すでに以下があります。

- Protocol草案
- CPU-only generator
- leakage audit
- Development：5 Worlds × 10,000 relations、計50,000 relations
- Sealed Holdout：5 Worlds × 10,000 relations、計50,000 relations
- Development 1,000 tasks
- Holdout 1,000 tasks
- private Gold分離
- Holdout `SEALED`ファイル
- solver未実行
- Holdout評価未実行
- leakage audit：`PASS / 0 issues`

つまり、**World設計と生成ロジック自体はほぼできています**。

## まだ足りないもの

### 1. 専用worktreeの実体

GitHub branchは作成済みです。

```text
rebuild/r6-multidimensional-query
```

ただしDevSpaceの認証エラーにより、専用worktreeの実作成は完了していません。現在は「branchあり、worktreeなし」です。

### 2. R6ファイルのbranchへの実コミット

現時点のbranchはまだ`main`の起点を指しており、次のR6ファイルがGitHub branch上に固定されていません。

```text
R6_MULTIDIMENSIONAL_QUERY_PROTOCOL.md
REBUILD_TRACK.md
r6/config/r6_generator_config.json
r6/schemas/*.schema.json
r6/src/generate_r6.py
r6/src/audit_r6_leakage.py
r6/src/r6_common.py
```

したがって、R0〜R5は確かに変更していませんが、**R6もまだGitHub上には載っていない状態**です。

### 3. `source_commit`が正しくない

現在の生成artifactには、次の`main`起点SHAが記録されています。

```text
7cf60630be8c0fe09b0c1ccb8a3832cbdee72cda
```

しかし本来は、**Protocol・config・schemas・generator・auditをコミットした後のR6 source commit SHA**を記録しなければなりません。

現在のSHAはR6ソースを含まないため、R0 Evidence Contract上は無効です。ここが最重要の修正点です。

### 4. audit実行後にsourceが変更されている

最初の監査を高速化するため、generatorとauditを後から修正しています。そのため現在、記録済みartifactと現行ソースで次のhash不一致があります。

```text
generate_r6.py
記録時: 650675c0...
現在値: 456c4829...

audit_r6_leakage.py
記録時: 683111d7...
現在値: 93964c7a...
```

つまり、audit結果自体は`PASS / 0 issues`ですが、**現在の最終ソースに対するPASSではありません**。

必要な順番は固定です。

```text
source freeze
→ Git commit
→ そのcommit SHAで再生成
→ leakage audit再実行
→ 全hash固定
```

### 5. 生成datasetとEvidenceが永続保存されていない

現在の生成物は一時作業領域にあります。

```text
/mnt/data/r6_test_output/r6gen-ddce798b9805adc0c3da/
```

ここには約100,000 relations、2,000 tasks、Gold、manifest、logs、hashesがありますが、まだ次のいずれにも保存されていません。

- GitHub branch
- GitHub Actions artifact
- 永続的なResearch artifact storage
- リリース用archive

一時ディレクトリだけでは第三者再現性を満たしません。

### 6. Protocol末尾の指定行

Protocol本文の最後に、指定された次の行がまだありません。

```text
R6_PROTOCOL_READY = YES / NO
```

現状を正確に反映するなら`NO`です。

### 7. R0完全準拠の再生成確認

leakage auditとは別に、同じcommit・config・seedで2回生成し、以下が完全一致することを確認する必要があります。

- dataset run ID
- relation files SHA256
- task files SHA256
- Gold files SHA256
- Holdout bundle commitment
- manifest hashes

これがないと「deterministic generatorを書いた」までは言えても、「決定論的再生成を実証した」とは言えません。

## 今回まだ不要なもの

以下は不足ではなく、**今は実施禁止または次段階**です。

- FULL_SCAN solverの実装
- SINGLE_DIM_QUERY solverの実装
- MULTI_DIM_QUERY solverの実装
- Task Success比較
- Developmentでの性能最適化
- Holdout評価
- Holdoutのcandidate set統計
- 結果を見たProtocol変更
- GPU実行

特にHoldoutは、生成・hash・構造的leakage監査までで止めるのが正しいです。

## 判定

World、task、Gold分離、seal、auditロジックはほぼ揃っています。しかし、**commit SHA・source hash・永続artifact・worktreeが未確定**なので、科学的なProtocol Freeze完了とはまだ言えません。

```text
R6_PROTOCOL_READY = NO
```

残作業の本質は研究設計ではなく、**最終ソースを先にコミットし、そのcommitから全データを再生成してEvidenceを閉じること**です。
````

## assistant / 2026-08-29T00:42:25.0000000Z

```
null
```

## user / 2026-08-29T02:36:38.0000000Z

```
担当：R6 Protocol Evidence Freeze

Branch:
rebuild/r6-multidimensional-query

目的：
R6研究内容を変更せず、R0 Evidence Contractに完全準拠したProtocol Freezeを完成させる。

新規研究・solver実装・Holdout評価は禁止。

現在のローカルR6 sourceを最終確認し、以下の順番を厳守する。

1. Dedicated worktree

rebuild/r6-multidimensional-query

の専用worktreeを実作成。

R0〜R5 worktree/codeは変更禁止。

2. Source Freeze

最低限以下をFreeze：

* R6_MULTIDIMENSIONAL_QUERY_PROTOCOL.md
* REBUILD_TRACK.md
* r6/config/r6_generator_config.json
* r6/schemas/*.schema.json
* r6/src/generate_r6.py
* r6/src/audit_r6_leakage.py
* r6/src/r6_common.py

結果を見てgenerator/auditを変更しない。

Protocol末尾はこの段階では：

R6_PROTOCOL_READY = NO

3. Source Commit

Freeze済みsourceをcommit。

このcommit SHAを唯一の：

source_commit

として以降の全artifactへ記録。

main起点SHAを使用禁止。

4. Full Regeneration

そのsource commitから新規に：

Development:

* 5 worlds
* 10,000 relations/world
* total 50,000 relations
* 1,000 tasks

Sealed Holdout:

* 5 worlds
* 10,000 relations/world
* total 50,000 relations
* 1,000 tasks

を再生成。

private Gold分離維持。

Holdout solver評価禁止。

5. Final Leakage Audit

最終commitのgenerator/auditに対して再実行。

最低限：

* Gold leakage
* Gold relation ID leakage
* answer leakage
* difficulty leakage
* generator-family leakage
* ordering leakage
* task-ID leakage
* Holdout seal integrity

PASS / issue countを保存。

6. Deterministic Regeneration Test

同一：

* source commit
* config
* seeds
* environment

で2回完全生成。

以下のSHA256が完全一致すること：

* Development relation files
* Development task files
* Development Gold
* Holdout relation files
* Holdout task files
* Holdout Gold/private bundle
* Holdout commitment
* manifests

run IDがcontent-derivedなら一致必須。
timestamp-derivedならdataset content IDを別途導入し、content hashes一致をPrimaryとする。

7. Source Hash Verification

manifest内に最終source filesのSHA256を保存し、現在working treeと完全一致確認。

旧一時artifactのsource hashesは使用しない。

8. Working Tree Gate

生成前後でtracked source treeがcleanであること。

生成artifactは専用artifact directoryへ保存し、source treeを汚染しない。

9. Persistent Evidence

一時 /mnt/data のみで終了禁止。

最低限、以下のどれかへ永続保存：

* branch上の小型manifest/hash/protocol
* GitHub Actions artifact
* private release/archive
* research artifact storage

大容量datasetをGitへ直接commitする必要はない。

ただし第三者がmanifestからartifactを取得・検証できる位置へ固定。

10. Final Evidence Commit

Evidence manifest / hashes / audit summary / regeneration proofをcommit。

Protocol末尾を：

R6_PROTOCOL_READY = YES

へ変更して最終commit。

Completion report

必須：

* worktree path
* source commit SHA
* evidence commit SHA
* source tree clean
* Development relation/task counts
* Holdout relation/task counts
* Holdout evaluated = false
* Gold separation status
* leakage audit PASS/FAIL + issues
* deterministic regeneration PASS/FAIL
* hashes matched / total
* persistent artifact location
* Development bundle SHA256
* Holdout commitment SHA256
* source hashes verification
* R6_PROTOCOL_READY = YES / NO

Solver実装禁止。
性能比較禁止。
Holdout開封評価禁止。
GPU禁止。
```

## assistant / 2026-08-29T02:36:40.0000000Z

```
R6の研究内容には触れず、指定順序どおり「worktree → source freeze/commit → 2回再生成 → audit → hash照合 → evidence固定 → final commit」だけを実施します。Holdoutは生成・封印・整合性確認のみで、solver/評価/GPUは一切実行しません。
```

## assistant / 2026-08-29T02:37:00.0000000Z

```
null
```

## assistant / 2026-08-29T02:37:06.0000000Z

```
null
```

## assistant / 2026-08-29T02:37:07.0000000Z

```
null
```

## assistant / 2026-08-29T02:37:12.0000000Z

```
null
```

## assistant / 2026-08-29T02:37:14.0000000Z

```
null
```

## assistant / 2026-08-29T02:37:17.0000000Z

```
null
```

## assistant / 2026-08-29T02:37:22.0000000Z

```
現時点の最重要ゲートは「前回の一時ローカルR6 sourceそのものを、変更せずsource commit化できるか」です。ここを推測で再作成すると研究内容変更になり得るため、元sourceの回収可否を先に再確認しています。
```

## assistant / 2026-08-29T02:37:26.0000000Z

```
null
```

## assistant / 2026-08-29T02:37:32.0000000Z

````
R6 Protocol Evidence Freezeは、**研究内容を変更せずに完了させるという条件を守ったため、今回はGate未通過で停止**しました。GitHub上の専用branch自体は存在しますが、現在もR6 sourceを含まず、`main`起点の `7cf60630be8c0fe09b0c1ccb8a3832cbdee72cda` を指しています。fileciteturn26file0L1-L6

原因は2点です。前回R6を生成した一時ローカル領域 `/mnt/data/r6_work` と `/mnt/data/r6_test_output/...` が現在の実行環境には残っておらず、さらに専用worktreeを操作していたDevSpace接続も現在利用できません。前回のR6 sourceを推測で再実装することは「現在のlocal R6 sourceをFreeze」「研究内容を変更しない」に違反するため実施していません。

### Completion report

| 項目 | 状態 |
|---|---|
| worktree path | **未作成 — Gate FAIL** |
| branch | `rebuild/r6-multidimensional-query` |
| source commit SHA | **未確定** |
| evidence commit SHA | **未作成** |
| branch current SHA | `7cf60630be8c0fe09b0c1ccb8a3832cbdee72cda`（使用禁止のmain起点SHA） |
| source tree clean | GitHub branch上はR6変更なし |
| Development relations | 前回一時生成では50,000、**今回Freeze evidenceとして未認定** |
| Development tasks | 前回1,000、**今回未認定** |
| Holdout relations | 前回50,000、**今回未認定** |
| Holdout tasks | 前回1,000、**今回未認定** |
| Holdout evaluated | **false** |
| Gold separation | 前回分離済み、**今回source-commit由来として未再検証** |
| leakage audit | 前回PASS / 0 issues、**最終source commitに対するauditではないため未認定** |
| deterministic regeneration | **NOT RUN** |
| hashes matched / total | **0 / required set — NOT VERIFIED** |
| persistent artifact location | **未固定** |
| Development bundle SHA256 | **未確定** |
| Holdout commitment SHA256 | **未確定** |
| source hashes verification | **FAIL / NOT POSSIBLE** |
| Solver implementation | **0** |
| Performance comparison | **0** |
| Holdout evaluation | **0** |
| GPU use | **0** |

R0のEvidence規則では、実artifact、source commit、dataset hash、raw logs等が必要であり、存在しない値を会話履歴から捏造してはいけません。fileciteturn8file0L2-L2 そのため、以前の一時生成結果を今回のFreeze成功として流用することもしていません。

```text
R6_PROTOCOL_READY = NO
```

**重要なのは、R6研究そのものには一切変更を加えていないことです。** branchにも「代替generator」や推測によるProtocolをコミットしていません。元のR6 sourceを再取得できる実行環境に接続できれば、指定された `source commit → 2回完全再生成 → audit → SHA一致 → persistent evidence → YES commit` の順序から再開できます。
````

## assistant / 2026-08-29T02:37:40.0000000Z

```
null
```

## assistant / 2026-08-29T02:37:40.0000000Z

```
null
```
